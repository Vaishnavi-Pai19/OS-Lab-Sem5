int main() {
    decl
        int temp;
        str input;
    enddecl
    
    begin
        temp = exposcall("Write", -2, "Filename:");
        temp = exposcall("Read", -1, input);
        temp = exposcall("Exec", input);
        return 0;
    end
}
